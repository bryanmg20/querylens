[pytest]
testpaths = telemetry_pipeline/tests
pythonpath = telemetry_pipeline
addopts = -ra
markers =
    contract: snapshot and queries contract tests
    unit: pure function tests
    integration: DB / queue tests